#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void)
{
	//int random;

	//srand((unsigned)time(NULL)); //unsigned쓰는 이유 : 시간은 음수가 아님
	//random = rand()%100+1; //0,1,2 -> 1,2,3
	//printf("%d", random);

	int com, 
	return 0;
}